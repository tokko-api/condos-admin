diff --git a/auth-api/src/main/java/com/condos/auth/config/CorsConfig.java b/shared-lib/src/main/java/com/condos/shared/web/CorsConfig.java
similarity index 74%
rename from auth-api/src/main/java/com/condos/auth/config/CorsConfig.java
rename to shared-lib/src/main/java/com/condos/shared/web/CorsConfig.java
index a33ad31..9df692f 100644
--- a/auth-api/src/main/java/com/condos/auth/config/CorsConfig.java
+++ b/shared-lib/src/main/java/com/condos/shared/web/CorsConfig.java
@@ -1,4 +1,4 @@
-package com.condos.auth.config;
+package com.condos.shared.web;
 
 import org.springframework.context.annotation.Bean;
 import org.springframework.context.annotation.Configuration;
@@ -9,6 +9,13 @@ import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
 import java.util.Arrays;
 import java.util.List;
 
+/**
+ * CORS compartido para todos los microservicios (auth-api, tenant-api, user-api, board-api, ...).
+ * Antes solo auth-api tenía esto definido localmente; se centraliza aquí para que
+ * cualquier servicio nuevo (p. ej. billing-api) lo herede sin duplicar código,
+ * y para que el resto de servicios (tenant-api, user-api, board-api) también
+ * acepten llamadas desde el frontend web (Expo en localhost:8081, condosadmin.site).
+ */
 @Configuration
 public class CorsConfig {
 
@@ -16,7 +23,6 @@ public class CorsConfig {
     public CorsConfigurationSource corsConfigurationSource() {
         CorsConfiguration config = new CorsConfiguration();
 
-        // 🔥 Agrega todos los orígenes que deben poder llamar auth-api
         config.setAllowedOrigins(List.of(
                 "http://localhost:3000",
                 "http://localhost:8081",
@@ -44,4 +50,4 @@ public class CorsConfig {
         source.registerCorsConfiguration("/**", config);
         return source;
     }
-}
\ No newline at end of file
+}
diff --git a/tenant-api/src/main/java/com/condos/tenant/config/SecurityConfig.java b/tenant-api/src/main/java/com/condos/tenant/config/SecurityConfig.java
index ae5d8a2..a59caa0 100644
--- a/tenant-api/src/main/java/com/condos/tenant/config/SecurityConfig.java
+++ b/tenant-api/src/main/java/com/condos/tenant/config/SecurityConfig.java
@@ -25,6 +25,7 @@ public class SecurityConfig {
                 .csrf(csrf -> csrf.disable())
                 .cors(Customizer.withDefaults())
                 .authorizeHttpRequests(auth -> auth
+                        .requestMatchers(HttpMethod.OPTIONS, "/**").permitAll()
                         .requestMatchers("/health", "/actuator/health").permitAll()
                         .requestMatchers(HttpMethod.GET, "/api/health").permitAll()
                         .anyRequest().authenticated()
diff --git a/user-api/src/main/java/com/condos/user/config/SecurityConfig.java b/user-api/src/main/java/com/condos/user/config/SecurityConfig.java
index d9e02cd..dcac889 100644
--- a/user-api/src/main/java/com/condos/user/config/SecurityConfig.java
+++ b/user-api/src/main/java/com/condos/user/config/SecurityConfig.java
@@ -6,7 +6,9 @@ import io.jsonwebtoken.Jws;
 import org.springframework.context.annotation.Bean;
 import org.springframework.context.annotation.Configuration;
 import org.springframework.http.HttpHeaders;
+import org.springframework.http.HttpMethod;
 import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
+import org.springframework.security.config.Customizer;
 import org.springframework.security.config.annotation.web.builders.HttpSecurity;
 import org.springframework.security.core.Authentication;
 import org.springframework.security.core.GrantedAuthority;
@@ -38,8 +40,10 @@ public class SecurityConfig {
     @Bean
     public SecurityFilterChain filterChain(HttpSecurity http, JwtService jwt) throws Exception {
         http.csrf(csrf -> csrf.disable());
+        http.cors(Customizer.withDefaults());
 
         http.authorizeHttpRequests(auth -> auth
+                .requestMatchers(HttpMethod.OPTIONS, "/**").permitAll()
                 .requestMatchers("/actuator/health").permitAll()   // healthcheck de Docker (igual que las otras APIs)
                 .requestMatchers("/internal/**").permitAll()
                 .requestMatchers("/condos/api/user/internal/**").permitAll()// libre para auth-api
